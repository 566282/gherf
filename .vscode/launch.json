{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "platform: dev server",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:5173",
      "webRoot": "${workspaceFolder}/platform",
      "preLaunchTask": "platform: dev"
    }
  ]
}